<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Manifest Manager</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <h2>Manifest Manager</h2>
      <nav class="nav">
        <a href="/orders" class="<%= (typeof activeNav !== 'undefined' && activeNav === 'orders') ? 'active' : '' %>">Orders</a>
        <a href="#" class="disabled">Manifests</a>
        <a href="#" class="disabled">Admin</a>
      </nav>
    </aside>
    <main class="content">
      <div class="container">
